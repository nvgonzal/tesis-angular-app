import { ServicioLoaderDirective } from './servicio-loader.directive';

describe('ServicioLoaderDirective', () => {
  it('should create an instance', () => {
    const directive = new ServicioLoaderDirective();
    expect(directive).toBeTruthy();
  });
});
